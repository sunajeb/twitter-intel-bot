name: LinkedIn Daily Monitor
on:
  schedule:
    # Run at 7:00 AM IST daily (1:30 AM UTC)
    # IST is UTC+5:30, so 7:00 AM IST = 1:30 AM UTC
    - cron: '30 1 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  linkedin-monitor:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Create config.json
      run: |
        echo '{
          "openai_api_key": "${{ secrets.OPENAI_API_KEY }}",
          "slack_webhook_url": "${{ secrets.SLACK_WEBHOOK_URL }}"
        }' > config.json

    - name: Run LinkedIn Monitor
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        SCRAPIN_API: ${{ secrets.SCRAPIN_API }}
      run: |
        echo "🔗 Starting LinkedIn daily monitor..."
        python linkedin_monitor.py
        
    - name: Log completion
      run: |
        echo "✅ LinkedIn monitor workflow completed at $(date)"
